# IMPORTANT: when JENKINS_ADMIN_PASSWORD is set, the setup wizard is SKIPPED, so its
# "install suggested plugins" step never runs -- THIS file is the only source of plugins.
# List everything you need (a bare Jenkins has almost none).
#
# Reproducibility: pin versions (id:version) if you want re-runs months apart to be identical;
# unpinned ids track the update centre's latest. Edit + re-run Update-Box.ps1 to apply (the install
# only runs when this file's contents change).
git
build-failure-analyzer
build-name-setter
build-timeout
credentials
dark-theme
cloudbees-folder
junit
nunit
throttle-concurrents
timestamper
text-finder

# pipeline
workflow-aggregator
pipeline-graph-view
pipeline-utility-steps
